import React from 'react';import {   LayoutDashboard,   CheckSquare,   Users,   Flag,   Archive,   HelpCircle,   Plus,   FolderKanban,  Sparkles,  Link, RotateCcw} from 'lucide-react';import { FilterState } from '../types';import logo from '../assets/images/logo.png';interface SidebarProps {  // filterState: FilterState;  // setFilterState: React.Dispatch<React.SetStateAction<FilterState>>;  // onOpenCreateModal: () => void;  // onResetData: () => void;}export const Sidebar: React.FC<SidebarProps> = ({}) => {  const navItems = [    {      id: 'overview' as const,      label: 'Tổng quan',      sublabel: 'Overview',      icon: LayoutDashboard,    },//     {//       id: 'myTasks' as const,//       label: 'Task của tôi',//       sublabel: 'My Tasks',//       icon: CheckSquare,//     },//     {//       id: 'teamBacklog' as const,//       label: 'Team Backlog',//       sublabel: 'All items',//       icon: Users,//     },//     {//       id: 'milestones' as const,//       label: 'Milestones',//       sublabel: 'Release roadmap',//       icon: Flag,//     },//     {//       id: 'archive' as const,//       label: 'Lưu trữ',//       sublabel: 'Archive',//       icon: Archive,//     },  ];  return (    <aside className="hidden md:flex bg-slate-900 text-slate-200 w-64 border-r border-slate-800 flex-col h-screen p-4 flex-shrink-0 select-none">      {/* Brand Header */}      <div className="flex items-center gap-3 px-2 py-3 mb-3 border-b border-slate-800">        <div className="w-10 h-10 rounded-lg flex items-center justify-center text-white font-bold shadow-md shadow-blue-600/30">          <img              src={logo}              alt="Logo"              className="w-10 h-10 rounded-lg object-contain"            />        </div>        <div className="overflow-hidden">          <h1 className="font-bold text-white text-base tracking-tight truncate flex items-center gap-1.5">            TaskFlow System          </h1>          <p className="text-xs text-slate-400 font-medium">Quản lý luồng công việc</p>        </div>      </div>      {/* Navigation Links */}      <nav className="flex flex-col gap-1 flex-grow">        {navItems.map((item) => {          const Icon = item.icon;          // const isActive = filterState.currentTab === item.id;          const isActive = '/';          return (            <button              key={item.id}              id={`nav-${item.id}`}              className={`flex items-center gap-3 px-3 py-2.5 rounded-lg text-sm font-medium transition-all text-left w-full cursor-pointer ${                isActive                  ? 'bg-blue-600 text-white shadow-sm shadow-blue-900/50'                  : 'text-slate-300 hover:bg-slate-800/80 hover:text-white'              }`}            >              <Icon className={`w-4 h-4 ${isActive ? 'text-white' : 'text-slate-400'}`} />              <div className="flex-1">                <div className="leading-none">{item.label}</div>              </div>            </button>          );        })}      </nav>      {/* Bottom Footer Info & Reset */}      <div className="pt-4 mt-auto flex flex-col gap-2 border-t border-slate-800">        {/* 1. KHỐI TRẠNG THÁI TÍCH HỢP (INTEGRATION CARD) */}        <div className="relative overflow-hidden bg-gradient-to-br from-slate-800/80 to-slate-900/40 rounded-xl p-3 border border-slate-700/50 shadow-sm">          {/* Ánh sáng glow mờ trang trí góc */}          <div className="absolute top-0 right-0 w-12 h-12 bg-blue-500/10 blur-xl rounded-full translate-x-1/3 -translate-y-1/3"></div>          <div className="flex items-center gap-2 mb-1.5 relative z-10">            <div className="w-5 h-5 rounded bg-blue-500/10 border border-blue-500/20 flex items-center justify-center">              <Link className="w-3 h-3 text-blue-400" />            </div>            <span className="text-xs font-semibold text-slate-200">Redmine Sync</span>          </div>          <p className="text-[10px] text-slate-400 leading-relaxed relative z-10">            Click vào Task để cập nhật URL & lên lịch đồng bộ.          </p>        </div>        {/* 2. NÚT CHỨC NĂNG (GHOST BUTTON) */}        <button            title="Khôi phục dữ liệu mẫu ban đầu"            className="group flex items-center justify-between w-full p-2 mt-1 text-xs font-medium text-slate-400 rounded-lg hover:bg-slate-800/60 hover:text-slate-200 transition-all duration-200"        >          <div className="flex items-center gap-2">            {/* Icon xoay ngược khi hover tạo cảm giác reset */}            <RotateCcw className="w-3.5 h-3.5 text-slate-500 group-hover:text-slate-300 group-hover:-rotate-90 transition-transform duration-300" />            <span>Khôi phục dữ liệu mẫu</span>          </div>        </button>        {/* 3. THÔNG TIN PHIÊN BẢN & TRẠNG THÁI HỆ THỐNG */}        <div className="flex items-center justify-between px-2 pt-2 mt-1 border-t border-slate-800/60 text-[10px] text-slate-500 font-medium">          <div className="flex items-center gap-1.5">            {/* Nút chấm xanh nhấp nháy báo hiệu hệ thống đang chạy */}            <span className="relative flex h-2 w-2">              <span className="animate-ping absolute inline-flex h-full w-full rounded-full bg-emerald-400 opacity-75"></span>              <span className="relative inline-flex rounded-full h-2 w-2 bg-emerald-500"></span>            </span>            <span>System Online</span>          </div>          <span className="font-mono tracking-wider">v1.0.0</span>        </div>      </div>    </aside>  );};